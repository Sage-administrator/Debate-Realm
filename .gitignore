# ============ Nuxt / Node 构建输出 ============
.output
.data
.nuxt
.nitro
.cache
dist

# ============ Prisma 生成客户端 ============
# 由 `prisma generate` 产出（约 5.8 万行），已在 package.json 的 postinstall 中自动生成。
# 不得提交：schema 每次变更都会产生海量无意义 diff。
# 注意：本目录同时已被 .prettierignore 与 eslint.config.mjs 排除。
server/lib/generated

# ============ Node 依赖 ============
node_modules

# ============ 日志 ============
logs
*.log

# ============ 备份 ============
*.db.bak*
prisma/*.db.bak*
.git.bak
*.bak

# ============ 敏感文件保护（环境变量/密钥/数据库） ============
.env
.env.*
!.env.example
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.sqlite-journal
*.sql
prisma/*.db
prisma/**/*.db
prisma/migrations_lock.toml
*.pem
*.key
*.cer
*.crt
*.p12
*.pfx

# ============ 参考项目 / 测试脚本 ============
reference/
test-api.ts
typecheck_log.txt

# ============ IDE / 编辑器设置 ============
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.fleet
.idea
.DS_Store
Thumbs.db
._*

# ============ CodeBuddy 本地配置（skills 例外，需团队共享） ============
.codebuddy/*
!.codebuddy/skills/
.codebuddy/skills/*.zip
.codebuddy/skills/debate-timer-sop/
.workbuddy/

# ============ 设计文档归档 ============
docs/ARCHITECTURE.md
docs/architecture.svg

# ============ 打包 / 杂项 ============
*.7z
nul
